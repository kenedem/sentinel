module.exports = {
  testEnvironment: "node",           // Run tests in Node.js environment
  moduleFileExtensions: ["js"],      // Recognize .js files
  testMatch: ["**/__tests__/**/*.test.js"], // Look for test files in __tests__ folder
  verbose: true                      // Show each test result in console
};