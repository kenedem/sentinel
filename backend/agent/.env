# API URL of backend server (make sure /api prefix is included in fetch paths)
NIACS_API_BASE=http://localhost:3001

# Agent info
NIACS_AGENT_ID=local-agent-1
NIACS_AGENT_NAME=Local Agent 1
NIACS_AGENT_REGION=us-east

# Token must match backend NIACS_AGENT_TOKEN
NIACS_AGENT_TOKEN=supersecrettoken# API URL of backend server (make sure /api prefix is included in fetch paths)
NIACS_API_BASE=http://localhost:3001

# Agent info
NIACS_AGENT_ID=local-agent-1
NIACS_AGENT_NAME=Local Agent 1
NIACS_AGENT_REGION=us-east

# Token must match backend NIACS_AGENT_TOKEN
NIACS_AGENT_TOKEN=supersecrettoken